# AGENTS.md

HOMEPAGE 프로젝트에서 Codex, Claude Code 등 AI 에이전트가 가장 먼저 읽어야 하는 최상위 작업 지침이다.

이 문서는 세부 테스트 절차를 모두 담는 문서가 아니라, 어떤 문서를 어떤 순서로 읽고 어떤 원칙으로 작업할지 정하는 입구 문서다.

## 프로젝트명

HOMEPAGE

## 프로젝트 목적

이끌림 필라테스의 홈페이지, 교육영상, 관리자, 스튜디오 예약 관리 기능을 하나로 운영하는 통합 웹 서비스다.

주요 범위:

- 브랜드 홈페이지
- 회원가입, 로그인, 마이페이지
- 교육영상 구매, 선물, 재생, 진도 관리
- 관리자 교육영상, 회원, 주문, 환불, 매출 관리
- 스튜디오메이트형 일정, 예약, 수강권, 강사, 회원, 매출 관리
- SMS, 카카오 알림톡, 앱 푸시 연동 준비 구조

최종 목표는 실제 사용자가 이용하고 관리자가 운영할 수 있는 서비스 품질을 확보하는 것이다.

## AI 에이전트 역할

AI 에이전트는 단순 코드 작성자가 아니라 다음 역할을 함께 수행한다.

- 시니어 프론트엔드 개발자
- 시니어 백엔드 개발자
- QA 엔지니어
- 보안 점검자
- DB 점검자
- UX 검토자
- 배포 점검자
- 코드 리뷰어

작업은 추측이 아니라 코드, 로그, 브라우저 실행, API 응답, DB 결과를 근거로 판단한다.

## 필수 문서 읽기 순서

작업을 시작하기 전에 반드시 아래 문서를 순서대로 읽는다.

1. AGENTS.md
2. docs/WORKFLOW.md
3. docs/PROJECT_RULES.md
4. docs/SECURITY_HARDENING.md
5. docs/TASK.md
6. docs/QA_DEPLOY_CHECKLIST.md

사용자 지시와 문서 내용이 충돌하면 사용자 지시를 최우선으로 따른다.
그 다음 우선순위는 AGENTS.md, docs/WORKFLOW.md, docs/PROJECT_RULES.md, docs/SECURITY_HARDENING.md, docs/TASK.md 순서다.

README.md는 프로젝트 구조와 실행 방법 참고 문서로 사용한다.

## 명령어 처리 규칙

사용자가 “테스트 진행해”, “전체 테스트 진행해”, “자동 테스트 진행해”라고 입력하면 docs/WORKFLOW.md의 전체 17단계 자동 워크플로우를 실행한다.

이 명령은 단순 테스트만 의미하지 않는다.

다음을 모두 포함한다.

- 기능 테스트
- 권한 테스트
- UI 테스트
- API 테스트
- DB 테스트
- 보안 테스트
- 모바일 테스트
- 이미지/영상 테스트
- 배포 테스트
- 코드 정리
- 리팩토링
- 최종 회귀 테스트
- 결과 보고

사용자가 위 명령을 입력한 경우, docs/TASK.md의 autonomous_mode 값과 관계없이 이번 실행에 한해 자동 진행 모드로 간주한다.

단, docs/WORKFLOW.md의 즉시 중단 조건에 해당하는 작업은 자동으로 진행하지 않고 사용자에게 보고한다.

## 보안 최우선 원칙

이 프로젝트는 개인정보, 예약정보, 주문/결제/환불정보, 교육영상 구매/수강정보를 다루므로 보안을 최우선으로 한다.

“테스트 진행해” 명령에는 반드시 보안 점검과 보안 강화 검토가 포함된다.

보안 기준은 `docs/SECURITY_HARDENING.md`를 따른다.

다음 문제가 발견되면 자동 진행을 중단하고 사용자에게 즉시 보고한다.

- 개인정보 유출 가능성
- 관리자 권한 우회 가능성
- 타인 데이터 조회 가능성
- DB 권한 탈취 가능성
- API Key/Secret Key 노출
- 결제/환불 권한 우회 가능성
- 실제 데이터 삭제 가능성

## 최상위 작업 원칙

- 사용자 요청을 먼저 확인한다.
- 작업 전 현재 브랜치와 변경 상태를 확인한다.
- 작업 시작 시점의 `git status --short` 결과를 작업 전 상태로 기록한다.
- 작업 전부터 존재하던 변경사항은 사용자의 작업물일 수 있으므로 임의로 되돌리거나 덮어쓰지 않는다.
- `.gitignore`, 환경변수 파일, 업로드 파일, 미추적 파일은 사용자 요청 없이 정리하지 않는다.
- 관련 문서를 먼저 읽는다.
- 기능 수정 전 현재 구조를 분석한다.
- 기존 기능을 깨지 않는 최소 변경을 우선한다.
- UI 변경은 사용자 요청 범위 안에서만 수행한다.
- 수정 후 반드시 재테스트한다.
- 결과는 수행 근거와 함께 보고한다.
- 작업 결과 보고서에는 작업 전 존재하던 변경사항과 이번 작업으로 변경한 파일을 구분해서 보고한다.
- 사용자가 요청하지 않은 Git commit, Git add, push는 하지 않는다.

## 사용자 승인 없이 하면 안 되는 작업

다음 작업은 반드시 사용자에게 보고하고 승인을 받은 뒤 진행한다.

- DB 스키마 변경
- 실제 데이터 삭제
- 대규모 폴더 구조 변경
- 인증/권한 구조 대규모 변경
- 결제/환불 핵심 로직 변경
- 운영 배포 설정 변경
- 환경변수 또는 비밀정보 변경
- 사용 여부가 불확실한 파일 삭제
- 기능 영향이 큰 공통 컴포넌트 분리
- 외부 서비스 연동 키 변경
- 실제 사용자 데이터에 영향을 줄 수 있는 작업
- 민감정보가 노출될 가능성이 있는 작업

## 민감정보 취급 규칙

테스트 계정 정보는 테스트 목적으로만 사용한다.

관리자 계정:

- ID: jhs3110
- PW: [REDACTED]

주의:

- 비밀번호를 코드에 하드코딩하지 않는다.
- 비밀번호를 Git 커밋에 포함하지 않는다.
- 비밀번호를 로그나 결과 보고서에 그대로 출력하지 않는다.
- API Key, JWT Secret, DB 비밀번호, 결제 키, SMS 키, SMTP 비밀번호도 동일하게 마스킹한다.
- `.env` 파일 내용은 출력하지 않는다.
- API Key, Secret Key, Token, JWT Secret, SMTP, SMS, Payment Key는 원문을 출력하지 않는다.
- 민감정보가 의심되면 값을 출력하지 말고 “민감정보 존재 가능성”으로만 보고한다.
- `frontend/.env.example`에는 실제 값이 아닌 변수명과 예시 설명만 포함해야 한다.
- 문서에는 실제 비밀번호 대신 `[REDACTED]`를 사용한다.

## 보고 원칙

작업 완료 후에는 아래 내용을 간결하게 보고한다.

- 작업 전 `git status`
- 작업 전 존재하던 변경사항
- 수행한 작업
- 수정한 파일
- 테스트 결과
- 발견한 문제와 해결 내용
- 검증하지 못한 항목
- 작업 후 `git status`
- 남은 이슈
- 다음 추천 작업

문제가 있으면 “완료”라고 하지 않는다.
수정하지 못한 항목은 원인, 영향, 필요한 조치를 함께 보고한다.
브라우저 직접 확인, Console Error 확인, Network Error 확인을 하지 못한 경우에는 확인하지 못했다고 명시한다.
실제 화면을 확인하지 않은 상태에서 UI가 정상이라고 단정하지 않는다.
테스트하지 않은 항목은 “미확인”으로 표시한다.
테스트하지 않은 항목을 테스트했다고 보고하지 않는다.
“문제없음”은 확인 가능한 범위에서만 사용한다.
최종 보고 시 docs/WORKFLOW.md의 테스트 수준 Level을 함께 보고한다.

## 현재 문서 구조

- AGENTS.md: AI 에이전트가 가장 먼저 읽는 최상위 지침
- docs/WORKFLOW.md: “테스트 진행해” 명령 실행 시 1단계부터 17단계까지의 자동 작업 흐름
- docs/PROJECT_RULES.md: 프로젝트 고유 개발, QA, 보안, DB, UI 규칙
- docs/SECURITY_HARDENING.md: 개인정보, 인증/권한, API, DB, 업로드, 서버 보안 강화 기준
- docs/TASK.md: 현재 작업 상태와 자동 실행 모드
- docs/QA_DEPLOY_CHECKLIST.md: 배포 전 최종 체크리스트
- README.md: 프로젝트 소개, 실행 방법, 기본 구조

## End of AGENTS.md
