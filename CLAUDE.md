# CLAUDE.md

Claude Code가 이 저장소에서 세션을 시작할 때 자동으로 읽는 파일이다.

**작업 지침의 본문은 `AGENTS.md` 하나로 관리한다.** 아래 한 줄이 그 내용을 그대로 불러온다. 규칙을 고칠 때는 `AGENTS.md`만 고치면 되고, 이 파일은 손댈 일이 없다.

@AGENTS.md

---

## 왜 이 파일이 따로 있나

`AGENTS.md`는 Codex 계열이 읽는 표준 파일명이고, Claude Code는 `CLAUDE.md`를 읽는다. 이 파일이 없으면 **Claude Code 세션이 프로젝트 규칙 없이 시작한다** — 필수 문서 읽기 순서도, 17단계 워크플로우도 모르는 상태로 코드를 건드리게 된다.

두 파일에 같은 내용을 복사해 두면 반드시 한쪽이 낡는다. 그래서 여기서는 `@AGENTS.md` import 한 줄만 둔다.
