# Local-only SSH tunnel settings for the shared AWS development environment.
# Copy this file to `.env.development.tunnel` on each Windows/macOS computer.
# Do not commit the copied file, private key, passwords, or session data.
ICL_DEV_ENVIRONMENT=development
ICL_DEV_DATABASE_NAME=homepage_dev

ICL_DEV_SSH_HOST=development-ec2.example.com
ICL_DEV_SSH_USER=ubuntu
ICL_DEV_SSH_KEY_PATH=~/Downloads/ICL-DEV-KEY.pem

# Frontend-only work: local Vite -> local 4001 -> development EC2 backend 4001.
ICL_DEV_API_LOCAL_PORT=4001
ICL_DEV_API_REMOTE_HOST=127.0.0.1
ICL_DEV_API_REMOTE_PORT=4001

# Backend work: local backend -> local 13306 -> development RDS 3306.
ICL_DEV_DB_LOCAL_PORT=13306
ICL_DEV_DB_REMOTE_HOST=development-db.example.rds.amazonaws.com
ICL_DEV_DB_REMOTE_PORT=3306
